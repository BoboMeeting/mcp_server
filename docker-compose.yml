services:
  mcpserver:
    build:
      context: .
      dockerfile: Dockerfile
    image: bobomeet-mcpserver:latest
    container_name: mcpserver
    ports:
      - "5103:8080"
    environment:
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
